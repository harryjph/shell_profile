[user]
	name = Harry Phillips
	email = harryjamesphillips@gmail.com
	signingkey = E4F581AA073CD264
[commit]
	gpgsign = true
[core]
	autocrlf = input
	editor = nano
[push]
	autoSetupRemote = true
# Generic config MUST go before site-specific config
[credential]
    helper = cache
    helper = bitwarden
[credential "https://github.com"]
# Blank helper resets the chain
	helper = 
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
# Blank helper resets the chain
    helper = 
	helper = !gh auth git-credential
